# The deployment section should be excluded until
# it is esxplicitlay included in module 5.
deployment:
  dockerhub:
      branch: master
      commands:
        - docker login -e keerti4p@gmail.com -u keerti4p -p sridevikrupa@4
        - docker push keerti4p/spring-web-demo


test:
  override:
    - mvn clean install

