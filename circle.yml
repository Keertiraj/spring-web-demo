# The deployment section should be excluded until
# it is explicitlay included in module 5.
deployment:
  dockerhub:
      branch: master
          commands:
        	- $DOCKERHUB

test:
  override:
    - mvn clean install

